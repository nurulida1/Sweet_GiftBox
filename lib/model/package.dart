class Package {
  String id, packageSet, description, quantity, price, images, date_reg;

  Package({
    required this.id,
    required this.packageSet,
    required this.description,
    required this.quantity,
    required this.price,
    required this.images,
    // ignore: non_constant_identifier_names
    required this.date_reg
  });
}

